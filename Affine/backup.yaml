apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: backup
  namespace: affine
spec:
  cluster:
    name: affine-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io