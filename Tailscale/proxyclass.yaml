apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: talos
spec:
  metrics:
    enable: true
    serviceMonitor:
      enable: true
  statefulSet:
    pod:
      labels:
        environment: prod
      nodeSelector:
        beta.kubernetes.io/os: "linux"